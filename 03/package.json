{
  "name": "node",
  "version": "1.0.0",
  "description": "``` docker run --rm --volume=$PWD:/srv/node -ti node npm install ```",
  "main": "index.js",
  "dependencies": {
    "react": "^16.4.2"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
